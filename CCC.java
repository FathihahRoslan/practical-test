//CCC concat AAA AND BBB
import java.io.*;
import java.net.*;

public String concat(String s) {
	{
	public static void main(String args[]) throws IOException
	
     ServerSocket BBB = new ServerSocket(2346);
	
      while(true) {
	Socket connectionsocket = BBB.accept();

        BufferedReader fromlb = new BufferedReader(new InputStreamReader(connectionsocket.getInputStream()));
			int i = s.length();
				if (i == 0) {
					return this;
					}
				else {
					char ac[] = new char[count + i];
						getChars(0, count, ac, 0);
						s.getChars(0, i, ac, count);
						return new String(0, count + i, ac);
						}
				
	String status=fromBBB.readLine();
	System.out.println("MODIFIED DATA " + status);
    
	}
}